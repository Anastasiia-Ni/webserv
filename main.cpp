#include "inc/Webserv.hpp"

int main(int argc, char **argv) {
    if (argc == 2) {
        //parser(argv[1]);
        //start;
        (void)argv;
    }
    else {
		std::cout << "error: wrong arguments" << std::endl;
		return (1);
	}
    return (0);
}
